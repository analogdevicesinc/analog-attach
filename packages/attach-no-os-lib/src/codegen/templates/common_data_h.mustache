#ifndef COMMON_DATA_H
#define COMMON_DATA_H

/* Includes */
{{#includes}}
#include "{{{.}}}"
{{/includes}}

/* Descriptors struct */
struct descriptors {
{{#descriptors}}
	struct {{{descriptor_type}}} *{{{descriptor_name}}};
{{/descriptors}}
};

extern struct descriptors desc;

/* Init param externs */
{{#externs}}
extern {{#is_const}}const {{/is_const}}struct {{{type}}} {{{name}}};
{{/externs}}

#endif /* COMMON_DATA_H */
