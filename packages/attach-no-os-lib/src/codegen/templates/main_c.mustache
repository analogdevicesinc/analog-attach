#include "no_os_error.h"
#include "common/common_data.h"
#include "user_app.h"

int main(void)
{
	int ret;

	// Patching the common_data structs
{{#runtime_assignments}}
	{{{struct_name}}}.{{{field_path}}} = {{{value}}};
{{/runtime_assignments}}

	// Init devices and structures
{{#devices}}
	ret = {{{init_code}}};
	if (ret)
		return ret;

{{/devices}}
	user_app(&desc);

{{#devices}}
	{{{remove_code}}};
{{/devices}}

	return 0;
}
