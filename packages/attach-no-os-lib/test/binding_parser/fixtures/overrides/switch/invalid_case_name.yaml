$id: "test/switch_invalid_case"
$type: struct
$symbol: test_struct
$ranking: 4
$sources:
  headers:
    - "test.h"

mode:
  type: enum
  values: [A, B]

value:
  type: uint8_t

$override:
  - $switch:
      $on: mode
      $cases:
        C:
          value:
            default: 100
