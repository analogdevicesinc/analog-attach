$id: "test/switch_numeric"
$type: struct
$symbol: test_struct
$ranking: 4
$sources:
  headers:
    - "test.h"

device_id:
  type: uint32_t

chip_select:
  type: uint32_t

$override:
  - $switch:
      $on: device_id
      $cases:
        0:
          chip_select:
            maximum: 1
        1:
          chip_select:
            maximum: 2
