$id: "test/if_then_no_value"
$type: struct
$symbol: test_struct
$ranking: 4
$sources:
  headers:
    - "test.h"

enabled:
  type: bool

count:
  type: uint32_t

$override:
  - $if:
      enabled:
        required: true
    $then:
      count:
        default: 0
