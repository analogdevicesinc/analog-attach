$id: "test/union_override"
$type: struct
$symbol: test_struct
$ranking: 4
$sources:
  headers:
    - "test.h"

comm:
  type: union
  members:
    - spi:
        include: "spi.yaml"
    - i2c:
        include: "i2c.yaml"

$override:
  - comm:
      value: spi
