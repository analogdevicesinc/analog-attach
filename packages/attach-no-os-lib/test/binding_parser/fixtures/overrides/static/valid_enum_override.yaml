$id: "test/enum_override"
$type: struct
$symbol: test_struct
$ranking: 4
$sources:
  headers:
    - "test.h"

mode:
  type: enum
  values: [MODE_A, MODE_B, MODE_C]

$override:
  - mode:
      default: MODE_B
      required: true
