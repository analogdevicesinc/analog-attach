$id: "test/union_missing_include.yaml"
$type: struct
$name: test_struct
$description: "Test struct with union containing missing include"
$ranking: 4
$sources:
  headers:
    - "test.h"

comm:
  type: union
  description: "Communication interface"
  members:
    - spi: { include: "no-os/no_os_spi_init_param.yaml" }
    - bad: { include: "does/not/exist.yaml" }
