# $if condition references a parent property that does not exist — never matches.
$id: "child.yaml"
$type: struct
$symbol: child
$ranking: 4
$sources: { noos: ["test.h"] }
$override:
  $parent:
    - $if:
        nonexistent:
          value: x
      $then:
        speed:
          minimum: 100
